{{ define "content" }}
<section class="space-y-4">
  <div>
    <h1 class="section-title">GopherGate</h1>
    <p class="section-subtitle">Secure WireGuard admin portal.</p>
  </div>

  <div class="card card-surface panel-glow max-w-2xl">
    <div class="card-body">
      <h2 class="card-title">UI Foundation Ready</h2>
      <p class="text-muted">
        Tailwind CSS, daisyUI, shared layout partials, and the global background system are now active.
      </p>
    </div>
  </div>
</section>
{{ end }}
